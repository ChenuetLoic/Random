{{ form_start(deleteForm) }}
    {{ form_widget(deleteForm) }}
<button class="btn btn-danger">{{ button_label|default('') }}<img src="https://img.icons8.com/stickers/30/000000/trash.png" alt="Supprimer"/></button>
{{ form_end(deleteForm) }}
